# Exp0001 — Qwen2.5-Omni-3B command examples
# Run all commands from: experiments/Exp0001-qwen25-3b/

# --- SLURM submission (validation, 400 samples) ---
TOTAL=400 bash slurm/submit_omni.sh

# --- SLURM submission (train, 7200 samples) ---
SPLIT=train TOTAL=7200 bash slurm/submit_omni.sh

# --- Override samples per task ---
TOTAL=400 SAMPLES_PER_TASK=10 bash slurm/submit_omni.sh

# --- Override walltime ---
TOTAL=400 bash slurm/submit_omni.sh --time=01:00:00

# --- Enable Flash Attention 2 (A100 GPUs) ---
TOTAL=400 FLASH_ATTN=1 bash slurm/submit_omni.sh

# --- Single task test (submit one array task only) ---
sbatch --array=0 \
  --export=ALL,MODEL_ID="Qwen/Qwen2.5-Omni-3B",MODEL_SHORT="qwen25-omni-3b",DATASET="BeTraC/betrac-2026",SPLIT="validation",FLASH_ATTN=0,SAMPLES_PER_TASK=4 \
  slurm/run_omni_hf.slurm

# --- Local run (all validation samples) ---
bash run_local.sh

# --- Local run (first 5 samples) ---
LIMIT=5 bash run_local.sh

# --- Eval set (requires access grant — see README.md) ---

# Pre-cache eval dataset on login node (needs HF_TOKEN with access)
../../steps/omni/.venv/bin/python -c "
from datasets import load_dataset
from huggingface_hub import snapshot_download
load_dataset('BeTraC/betrac-2026-eval', split='eval')
snapshot_download('Qwen/Qwen2.5-Omni-3B')
"

# Single task test
sbatch --array=0 \
  --export=ALL,MODEL_ID="Qwen/Qwen2.5-Omni-3B",MODEL_SHORT="qwen25-omni-3b",DATASET="BeTraC/betrac-2026-eval",SPLIT="eval",FLASH_ATTN=0,SAMPLES_PER_TASK=4 \
  slurm/run_omni_hf.slurm

# Full eval submission (875 samples)
DATASET=BeTraC/betrac-2026-eval SPLIT=eval TOTAL=875 SAMPLES_PER_TASK=20 SKIP_CACHE=1 \
  bash slurm/submit_omni.sh

# Local eval run
DATASET=BeTraC/betrac-2026-eval SPLIT=eval bash run_local.sh
